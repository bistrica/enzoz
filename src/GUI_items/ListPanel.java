package GUI_items;

import items.Item;
//import items.PozycjaNaRecepcie;

import java.awt.Component;
//import java.util.ArrayList;

import javax.swing.BoxLayout;
import javax.swing.JPanel;

public class ListPanel<Ty extends Panel> extends JPanel {

	/*
	
	public ListPanel() {
		//items=new ArrayList<DrugPanel>();
		setLayout(new BoxLayout(this, BoxLayout.Y_AXIS));
		
	}
	
	public void add(Item pos) {
		for (Component panel: getComponents()) {
			if (((Ty)panel).getPosition().equals(pos))
				return;
		}
		add(new Class<Ty>(pos));
		revalidate();
		//return positions.contains(pos);
	}
*/	
}
